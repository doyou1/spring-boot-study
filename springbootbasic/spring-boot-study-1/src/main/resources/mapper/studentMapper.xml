<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jh.test.mapper.StudentMapper">
    <insert id="insertStudent" parameterType="student">
        insert into student(
            name
            ,code
            ,save_date
        ) values (
            #{name}
            ,#{code}
            ,#{saveDate}
        )
    </insert>
    <select id="getStudents" resultType="student">
        select
            id
            ,name
            ,code
            ,save_date as 'saveDate'
        from
            student
    </select>
    <select id="getStudentByCode" parameterType="String" resultType="student">
        select
            id
            ,name
            ,code
            ,save_date
    </select>

    <update id="updateStudent" parameterType="student">
        update
            student
        set
            name = #{name}
            ,code = #{code}
            ,save_date = #{saveDate}
        where
            id = #{id}
    </update>
    <delete id="deleteStudentById" parameterType="int">
        delete
        from
            student
        where
            id = #{id}
    </delete>
</mapper>